[defaults]
inventory = inventory
roles_path = roles
collections_paths = collections
remote_user = alanher
ask_pass = false
log_path = logs/ansible.log
[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
